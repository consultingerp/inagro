<odoo>
  <data>
    <!-- explicit list view definition -->
    <!--
    <record model="ir.ui.view" id="dn_enterprise.list">
      <field name="name">dn_enterprise list</field>
      <field name="model">dn_enterprise.dn_enterprise</field>
      <field name="arch" type="xml">
        <tree>
          <field name="name"/>
          <field name="value"/>
          <field name="value2"/>
        </tree>
      </field>
    </record>
    -->

    <!-- actions opening views on models -->
    <!--
    <record model="ir.actions.act_window" id="dn_enterprise.action_window">
      <field name="name">dn_enterprise window</field>
      <field name="res_model">dn_enterprise.dn_enterprise</field>
      <field name="view_mode">tree,form</field>
    </record>
    -->

    <!-- server action to the one above -->
    <!--
    <record model="ir.actions.server" id="dn_enterprise.action_server">
      <field name="name">dn_enterprise server</field>
      <field name="model_id" ref="model_dn_enterprise_dn_enterprise"/>
      <field name="state">code</field>
      <field name="code">
        action = {
          "type": "ir.actions.act_window",
          "view_mode": "tree,form",
          "res_model": self._name,
        }
      </field>
    </record>
    -->

    <!-- Top menu item -->
    <!--
    <menuitem name="dn_enterprise" id="dn_enterprise.menu_root"/>
    -->
    <!-- menu categories -->
    <!--
    <menuitem name="Menu 1" id="dn_enterprise.menu_1" parent="dn_enterprise.menu_root"/>
    <menuitem name="Menu 2" id="dn_enterprise.menu_2" parent="dn_enterprise.menu_root"/>
    -->
    <!-- actions -->
    <!--
    <menuitem name="List" id="dn_enterprise.menu_1_list" parent="dn_enterprise.menu_1"
              action="dn_enterprise.action_window"/>
    <menuitem name="Server to list" id="dn_enterprise" parent="dn_enterprise.menu_2"
              action="dn_enterprise.action_server"/>
    -->
  </data>
</odoo>